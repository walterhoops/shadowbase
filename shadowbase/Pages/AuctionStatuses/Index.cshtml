@page
@model shadowbase.Pages.AuctionStatuses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AuctionStatus[0].AuctionStatusDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AuctionStatus) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AuctionStatusDescription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AuctionStatusID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AuctionStatusID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AuctionStatusID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
